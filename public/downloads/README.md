APK files are gitignored. Deploy via **Google Cloud Run** ([`docs/DEPLOY-GCP.md`](../../docs/DEPLOY-GCP.md)) with the APK present in this folder, or set `NEXT_PUBLIC_APP_APK_URL` to an external CDN URL.